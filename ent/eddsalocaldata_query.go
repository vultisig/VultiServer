// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"vultisigner/ent/eddsalocaldata"
	"vultisigner/ent/predicate"
	"vultisigner/ent/vault"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// EddsaLocalDataQuery is the builder for querying EddsaLocalData entities.
type EddsaLocalDataQuery struct {
	config
	ctx        *QueryContext
	order      []eddsalocaldata.OrderOption
	inters     []Interceptor
	predicates []predicate.EddsaLocalData
	withVault  *VaultQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EddsaLocalDataQuery builder.
func (eldq *EddsaLocalDataQuery) Where(ps ...predicate.EddsaLocalData) *EddsaLocalDataQuery {
	eldq.predicates = append(eldq.predicates, ps...)
	return eldq
}

// Limit the number of records to be returned by this query.
func (eldq *EddsaLocalDataQuery) Limit(limit int) *EddsaLocalDataQuery {
	eldq.ctx.Limit = &limit
	return eldq
}

// Offset to start from.
func (eldq *EddsaLocalDataQuery) Offset(offset int) *EddsaLocalDataQuery {
	eldq.ctx.Offset = &offset
	return eldq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (eldq *EddsaLocalDataQuery) Unique(unique bool) *EddsaLocalDataQuery {
	eldq.ctx.Unique = &unique
	return eldq
}

// Order specifies how the records should be ordered.
func (eldq *EddsaLocalDataQuery) Order(o ...eddsalocaldata.OrderOption) *EddsaLocalDataQuery {
	eldq.order = append(eldq.order, o...)
	return eldq
}

// QueryVault chains the current query on the "vault" edge.
func (eldq *EddsaLocalDataQuery) QueryVault() *VaultQuery {
	query := (&VaultClient{config: eldq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := eldq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := eldq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(eddsalocaldata.Table, eddsalocaldata.FieldID, selector),
			sqlgraph.To(vault.Table, vault.FieldID),
			sqlgraph.Edge(sqlgraph.O2O, false, eddsalocaldata.VaultTable, eddsalocaldata.VaultColumn),
		)
		fromU = sqlgraph.SetNeighbors(eldq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first EddsaLocalData entity from the query.
// Returns a *NotFoundError when no EddsaLocalData was found.
func (eldq *EddsaLocalDataQuery) First(ctx context.Context) (*EddsaLocalData, error) {
	nodes, err := eldq.Limit(1).All(setContextOp(ctx, eldq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{eddsalocaldata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) FirstX(ctx context.Context) *EddsaLocalData {
	node, err := eldq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EddsaLocalData ID from the query.
// Returns a *NotFoundError when no EddsaLocalData ID was found.
func (eldq *EddsaLocalDataQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eldq.Limit(1).IDs(setContextOp(ctx, eldq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{eddsalocaldata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) FirstIDX(ctx context.Context) int {
	id, err := eldq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EddsaLocalData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EddsaLocalData entity is found.
// Returns a *NotFoundError when no EddsaLocalData entities are found.
func (eldq *EddsaLocalDataQuery) Only(ctx context.Context) (*EddsaLocalData, error) {
	nodes, err := eldq.Limit(2).All(setContextOp(ctx, eldq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{eddsalocaldata.Label}
	default:
		return nil, &NotSingularError{eddsalocaldata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) OnlyX(ctx context.Context) *EddsaLocalData {
	node, err := eldq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EddsaLocalData ID in the query.
// Returns a *NotSingularError when more than one EddsaLocalData ID is found.
// Returns a *NotFoundError when no entities are found.
func (eldq *EddsaLocalDataQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = eldq.Limit(2).IDs(setContextOp(ctx, eldq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{eddsalocaldata.Label}
	default:
		err = &NotSingularError{eddsalocaldata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) OnlyIDX(ctx context.Context) int {
	id, err := eldq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EddsaLocalDataSlice.
func (eldq *EddsaLocalDataQuery) All(ctx context.Context) ([]*EddsaLocalData, error) {
	ctx = setContextOp(ctx, eldq.ctx, "All")
	if err := eldq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EddsaLocalData, *EddsaLocalDataQuery]()
	return withInterceptors[[]*EddsaLocalData](ctx, eldq, qr, eldq.inters)
}

// AllX is like All, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) AllX(ctx context.Context) []*EddsaLocalData {
	nodes, err := eldq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EddsaLocalData IDs.
func (eldq *EddsaLocalDataQuery) IDs(ctx context.Context) (ids []int, err error) {
	if eldq.ctx.Unique == nil && eldq.path != nil {
		eldq.Unique(true)
	}
	ctx = setContextOp(ctx, eldq.ctx, "IDs")
	if err = eldq.Select(eddsalocaldata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) IDsX(ctx context.Context) []int {
	ids, err := eldq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (eldq *EddsaLocalDataQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, eldq.ctx, "Count")
	if err := eldq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, eldq, querierCount[*EddsaLocalDataQuery](), eldq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) CountX(ctx context.Context) int {
	count, err := eldq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (eldq *EddsaLocalDataQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, eldq.ctx, "Exist")
	switch _, err := eldq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (eldq *EddsaLocalDataQuery) ExistX(ctx context.Context) bool {
	exist, err := eldq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EddsaLocalDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (eldq *EddsaLocalDataQuery) Clone() *EddsaLocalDataQuery {
	if eldq == nil {
		return nil
	}
	return &EddsaLocalDataQuery{
		config:     eldq.config,
		ctx:        eldq.ctx.Clone(),
		order:      append([]eddsalocaldata.OrderOption{}, eldq.order...),
		inters:     append([]Interceptor{}, eldq.inters...),
		predicates: append([]predicate.EddsaLocalData{}, eldq.predicates...),
		withVault:  eldq.withVault.Clone(),
		// clone intermediate query.
		sql:  eldq.sql.Clone(),
		path: eldq.path,
	}
}

// WithVault tells the query-builder to eager-load the nodes that are connected to
// the "vault" edge. The optional arguments are used to configure the query builder of the edge.
func (eldq *EddsaLocalDataQuery) WithVault(opts ...func(*VaultQuery)) *EddsaLocalDataQuery {
	query := (&VaultClient{config: eldq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	eldq.withVault = query
	return eldq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Xi string `json:"xi,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EddsaLocalData.Query().
//		GroupBy(eddsalocaldata.FieldXi).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (eldq *EddsaLocalDataQuery) GroupBy(field string, fields ...string) *EddsaLocalDataGroupBy {
	eldq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EddsaLocalDataGroupBy{build: eldq}
	grbuild.flds = &eldq.ctx.Fields
	grbuild.label = eddsalocaldata.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Xi string `json:"xi,omitempty"`
//	}
//
//	client.EddsaLocalData.Query().
//		Select(eddsalocaldata.FieldXi).
//		Scan(ctx, &v)
func (eldq *EddsaLocalDataQuery) Select(fields ...string) *EddsaLocalDataSelect {
	eldq.ctx.Fields = append(eldq.ctx.Fields, fields...)
	sbuild := &EddsaLocalDataSelect{EddsaLocalDataQuery: eldq}
	sbuild.label = eddsalocaldata.Label
	sbuild.flds, sbuild.scan = &eldq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EddsaLocalDataSelect configured with the given aggregations.
func (eldq *EddsaLocalDataQuery) Aggregate(fns ...AggregateFunc) *EddsaLocalDataSelect {
	return eldq.Select().Aggregate(fns...)
}

func (eldq *EddsaLocalDataQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range eldq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, eldq); err != nil {
				return err
			}
		}
	}
	for _, f := range eldq.ctx.Fields {
		if !eddsalocaldata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if eldq.path != nil {
		prev, err := eldq.path(ctx)
		if err != nil {
			return err
		}
		eldq.sql = prev
	}
	return nil
}

func (eldq *EddsaLocalDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EddsaLocalData, error) {
	var (
		nodes       = []*EddsaLocalData{}
		_spec       = eldq.querySpec()
		loadedTypes = [1]bool{
			eldq.withVault != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EddsaLocalData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EddsaLocalData{config: eldq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, eldq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := eldq.withVault; query != nil {
		if err := eldq.loadVault(ctx, query, nodes, nil,
			func(n *EddsaLocalData, e *Vault) { n.Edges.Vault = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (eldq *EddsaLocalDataQuery) loadVault(ctx context.Context, query *VaultQuery, nodes []*EddsaLocalData, init func(*EddsaLocalData), assign func(*EddsaLocalData, *Vault)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*EddsaLocalData)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
	}
	query.withFKs = true
	query.Where(predicate.Vault(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(eddsalocaldata.VaultColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.eddsa_local_data_vault
		if fk == nil {
			return fmt.Errorf(`foreign-key "eddsa_local_data_vault" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "eddsa_local_data_vault" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (eldq *EddsaLocalDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := eldq.querySpec()
	_spec.Node.Columns = eldq.ctx.Fields
	if len(eldq.ctx.Fields) > 0 {
		_spec.Unique = eldq.ctx.Unique != nil && *eldq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, eldq.driver, _spec)
}

func (eldq *EddsaLocalDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(eddsalocaldata.Table, eddsalocaldata.Columns, sqlgraph.NewFieldSpec(eddsalocaldata.FieldID, field.TypeInt))
	_spec.From = eldq.sql
	if unique := eldq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if eldq.path != nil {
		_spec.Unique = true
	}
	if fields := eldq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, eddsalocaldata.FieldID)
		for i := range fields {
			if fields[i] != eddsalocaldata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := eldq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := eldq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := eldq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := eldq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (eldq *EddsaLocalDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(eldq.driver.Dialect())
	t1 := builder.Table(eddsalocaldata.Table)
	columns := eldq.ctx.Fields
	if len(columns) == 0 {
		columns = eddsalocaldata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if eldq.sql != nil {
		selector = eldq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if eldq.ctx.Unique != nil && *eldq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range eldq.predicates {
		p(selector)
	}
	for _, p := range eldq.order {
		p(selector)
	}
	if offset := eldq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := eldq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EddsaLocalDataGroupBy is the group-by builder for EddsaLocalData entities.
type EddsaLocalDataGroupBy struct {
	selector
	build *EddsaLocalDataQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eldgb *EddsaLocalDataGroupBy) Aggregate(fns ...AggregateFunc) *EddsaLocalDataGroupBy {
	eldgb.fns = append(eldgb.fns, fns...)
	return eldgb
}

// Scan applies the selector query and scans the result into the given value.
func (eldgb *EddsaLocalDataGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eldgb.build.ctx, "GroupBy")
	if err := eldgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EddsaLocalDataQuery, *EddsaLocalDataGroupBy](ctx, eldgb.build, eldgb, eldgb.build.inters, v)
}

func (eldgb *EddsaLocalDataGroupBy) sqlScan(ctx context.Context, root *EddsaLocalDataQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eldgb.fns))
	for _, fn := range eldgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eldgb.flds)+len(eldgb.fns))
		for _, f := range *eldgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eldgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eldgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EddsaLocalDataSelect is the builder for selecting fields of EddsaLocalData entities.
type EddsaLocalDataSelect struct {
	*EddsaLocalDataQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (elds *EddsaLocalDataSelect) Aggregate(fns ...AggregateFunc) *EddsaLocalDataSelect {
	elds.fns = append(elds.fns, fns...)
	return elds
}

// Scan applies the selector query and scans the result into the given value.
func (elds *EddsaLocalDataSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, elds.ctx, "Select")
	if err := elds.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EddsaLocalDataQuery, *EddsaLocalDataSelect](ctx, elds.EddsaLocalDataQuery, elds, elds.inters, v)
}

func (elds *EddsaLocalDataSelect) sqlScan(ctx context.Context, root *EddsaLocalDataQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(elds.fns))
	for _, fn := range elds.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*elds.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := elds.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
